# Day 2.2 samples — XSLT 3.0

Samples for the enrichment XSL lab on `<CanonicalOrder>`.

| File | Purpose |
|---|---|
| `canonical_sample.xml` | Input to the XSLT step (same as yesterday's mapping output) |
| `enriched_expected.xml` | Expected output after enrichment |
| `xslt_EnrichCanonicalOrder.xsl` | Full reference stylesheet (drop into iFlow Resources) |
| `xslt_identity_starter.xsl` | Bare identity transform — copy-paste seed for new stylesheets |
| `xpath3_cheatsheet.md` | XPath 3.1 constructs you'll actually reach for |
| `exf_extension_quickref.md` | `exf:get/setHeader`, `exf:get/setProperty` patterns + ordering gotcha |
| `failure_cases.md` | Empty currency / missing header / wrapped root — what happens and why |
